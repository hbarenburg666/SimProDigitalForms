# Secrets — NEVER commit
.env
*.env
!.env.example

# Token cache (contains bearer tokens)
.token_cache.json

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Form dumps may contain company data — opt in deliberately
form_dump.json
dumps/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
copy_payload.json
copy_result.json

# Source QMS documents — company controlled, not for GitHub
pdfs/
dump_*.json
minmax_payload_*.json
minmax_result_*.json
api4000_payload.json
maskfix_payload_*.json
